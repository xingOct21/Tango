# DEVLOG

## v2.4 — 2026-08-25

### 修复：斩词上线近两个月，一次都没触发过

现象是"熟悉度满格的确认弹窗从来没出现过"，但斩词本身没坏 —— 后端的
`reached_max_level`（`app.py` 的 `/api/review`）和前端的 `confirm`（`index.html`）
都是对的，v2.1 那次离线全流程测试也确实跑通过。问题在上游：**level 4→5 的那次
复习永远排不到队**，触发条件从来没有被满足过。

> **阅读提示**：紧接着的两段描述的是**修复前**的行为，不是现状。当前代码里复习队列按
> `(next_review, level)` 排序、且复习池未清空时不发新词 —— 即本节下方「`app.py`」那两条
> 改动。核对现状一律以 `app.py` 的 `/api/next` 为准，别把这两段当当前逻辑读。

复习队列此前按熟悉度升序排（`review_due.sort(key=lambda x: x["level"])`），每次请求
只发队首一个。而一个词从 0 升到满级需要 4 次复习（1→2、2→3、3→4、4→5），10 新词/天
意味着稳态复习需求约 40 次/天，长期高于默认的 20 次/天复习额度。缺口 100% 落在队尾
—— 也就是熟悉度最高的那批词身上，于是它们永远排不到、也永远升不到 level 5。

两个因素让它更糟：复习额度用完后仍然继续发新词（新词分支是独立判断的），每天往队首
再灌 10 个 level 1 的词，积压只增不减；被点过"不会"而掉回 level 0 的词间隔是 0 天，
次日必然到期、永远站在队首，持续挤占额度。

线上数据印证：`SELECT level, count(*) FROM word_progress WHERE book='jp' GROUP BY level`
的结果是等级只到 4，大量堆在 2 和 3，只有 1 个词摸到过 4，没有任何 level 5。

- **`app.py`** —— 复习队列排序由 `level` 升序改为 `(next_review, level)`：逾期最久的先出，
  同一天到期的再按熟悉度从低到高；`review_due` 的元素随之带上 `next_review`
- **`app.py`** —— 还有到期未复习的词时不发新词（新词分支加 `and not review_due`）。
  等积压清完，新词自然恢复
- 只动 `/api/next` 的读路径，不涉及 schema、迁移或写入，已有进度数据不受影响

### 验证

离线模拟：内存假 Supabase 客户端 + 假时钟，直接调 `app.py` 的视图函数（只桩掉 Flask
和 supabase 的胶水层，逻辑是真的），读真实 `word.md`，用户每次都答"记住了"、弹窗后
选"斩" —— 即最理想情况。

- 修复前，10 新词 / 20 复习 / 58 天：最高只到 level 3，540 个词卡在那里，斩词弹窗 0 次，
  到期未发出的积压 470
- 修复后，同样参数、新用户从零开始 58 天：最高 level 5，**第 27 天**首次弹出斩词，
  积压 0。第 27 天与理论最短值吻合 —— 升满级的四次复习间隔是 1+3+7+14=25 天
- 反向验证（确认测试本身有效）：**不改代码**、只把复习额度提到 35 以上，斩词就能触发
  （35 → 165 次，40 → 330 次）。证明瓶颈是"队列顺序 + 额度"的组合，不是斩词代码本身

### 遗留：修复前攒下的积压需要手动清

"停发新词"会让修复前积累的旧账变得可见 —— 不是这次改动造成的，但它会挡住新词。
按"大量在 level 2/3、约 580 词、全部已逾期"估算（线上库无凭据可核，绝对条数是估的，
但结论形状不依赖具体条数）：

- 复习额度 20：第 28 天首次斩词，第 73 天才恢复发新词
- 复习额度 40：第 14 天首次斩词，第 35 天恢复
- 复习额度 60：第 9 天首次斩词，第 14 天恢复

建议在设置里临时把复习额度调到 40~60，把这批旧账清掉之后再调回来。

### 文档更正

- **`TECH_STACK.md`** —— "复习池内部按熟悉度从低到高推送"随本次改动失效，改为按到期日排序

## v2.3 — 2026-08-10

### 修复：一本额度用完后切不到另一本

现象是"日语和英语的每日额度互相污染"，实际原因在前端，跟额度无关。

`#jp` / `#answer` / `#kana` / `#level-bar` / `#section-tag` 都是 `#card` 的子元素，
而额度用完时用 `card.innerHTML = ...` 覆盖整张卡片，把这些元素从 DOM 里删掉了。
之后每次 `loadNext()` 走到 `getElementById("answer").style.display` 都会对 null
取属性抛异常、函数中断，于是切单词本、「继续学习」、「复习」、「返回」全部失效，
页面卡在"额度已用完"那张卡片上 —— 看起来就像另一本也被限制了。手动刷新即恢复，
所以一直被当成额度问题。

这个缺陷**从 v2.0 交付当天就存在**，已在 `330e7e4` 核实：那一版 `#answer` 同样是
`#card` 的子元素，`loadNext()` 同样在 `data.done` 判断之前就访问它，done 分支同样
覆盖 `#card.innerHTML`。也就是说 v2.0 新增的「继续学习」和「自由复习」两个按钮
——它们只能从"已用完"画面进入——**交付起就从未真正工作过**：
点「继续学习」其实已经写进 localStorage 了，只是画面不刷新，手动刷新页面反而"生效"，
所以一直没被发现。详见下面 v2.0 条目的更正。

- **`index.html`** —— 新增独立的 `#done-msg` 提示元素；正面元素改为 `display:none`
  隐藏而非删除，由 `showDoneMessage()` / `showWordFace()` 成对切换。自由复习结束那处
  同样的覆盖写法一并改掉。现在文件里没有任何 `getElementById("card")` 的覆盖

### 修复：单词本之间的进度互相覆盖

`word_progress` 的主键一直是 `jp` 单列 —— v2.2 加了 `book` 列却没改主键。
`save_progress` 的 upsert 因此按 `jp` 冲突合并：同一条文本在两个单词本之间会互相
覆盖，upsert 把已有行的 `book` 直接改掉，等于把进度从一本搬到另一本，`level` 和
`review_count` 一并重置。同时这也意味着一条文本永远只能存在于一个本里。

- **`schema.sql`** —— 建表主键改为 `(jp, book)`；新增 v2.3 迁移段
- **`app.py`** —— upsert 显式指定 `on_conflict="jp,book"`
- **执行顺序有硬依赖**：必须先在 Supabase 改主键，再部署新代码。反过来会让
  `ON CONFLICT (jp, book)` 找不到匹配的唯一约束，Postgres 报 42P10，`/api/review` 500

### 修复：PostgREST 行数上限导致进度被静默截断

`get_progress` 等四处直接 `select()`，超过服务端上限（Supabase 默认 1000 行）的部分
会被悄悄丢掉，被截掉的词重新变成"没学过的新词"。`word.md` 已有 1146 条，正逼近这个上限。

- **`app.py`** —— 新增 `fetch_all()` 按 `range` 分页取全量，翻到空页为止、按实际返回
  条数推进游标，因此不依赖上限具体是多少。`get_progress` / 两个当日计数 /
  `review_today` 四处改用它

### 修复：额度用完的文案误报

`/api/next` 的 `reason` 用 `or` 串联三个条件，新词额度已满、复习额度尚有剩余时会
误报成"所有到期单词复习完了"。改为只有"还有词可出、但被额度挡住"才算 `daily_limit`。

### 修复：「继续学习」状态不分单词本

`tango_extended_date` 是单一全局 key，在一本里点了展开会带到另一本 / 切回来又丢掉。
改为按本存 `tango_extended_{book}`，并在启动时清掉旧 key。

### 排查记录

先后验证并**排除**了两个方向，记下来避免以后重复走：

- **迁移把英语行错标成日语** —— v2.2 之前有一段时间（`c4a3d51` → `7c9c805`，约 19 小时）
  英语本已能出题但后端还不认 book，那期间写入的行没有 `book` 列，理论上会被
  `ADD COLUMN book ... DEFAULT 'jp'` 盖成日语本。实测查询返回空，库里没有这种脏数据。
  另外 v2.2 当时用 `SELECT book, count(*) GROUP BY book` 做的验证是**恒真**的
  —— `NOT NULL DEFAULT 'jp'` 保证了它只可能返回 `jp`，这条查询不具备证伪能力
- **旧代码的全局额度键** —— 旧版设置弹窗 `fetch("/api/settings")` 不带 book，读写共享的
  全局键。但线上确实已是新代码，排除

### 验证

- 离线回归测试（内存假 Supabase 客户端，模拟 PostgREST 的 upsert 冲突语义和行数上限）
  12 项全通过；并反向验证过测试本身有效：旧主键下前两组用例确实失败
- 上述"新代码 + 旧主键 = 42P10"的顺序依赖，是测试实际跑出来的，不是推测
- 服务端行数上限分别按 1000 和 300 跑过，确认分页逻辑不依赖上限取值

## v2.2 — 2026-08-08

### 已完成（已推送 `c4a3d51`）
- 新增英语答辩问答本 `word_en.md`，结构为「问题 / 英文答案 / 中文答案」，问题与答案均中英对照，段落用字面量 `\n` 分隔，前端 `renderText()` 渲染为换行
- UI 加入「日语 / English」单词本切换，选择存 `localStorage`；答辩本在翻牌前只显示问题，翻牌后同时显示英文答案与中文
- `parser.parse_words(book)` 按 `book` 参数读取 `word.md` 或 `word_en.md`；`/api/next`、`/api/review_today` 接受 `book` 查询参数
- 新增 `CLAUDE.md` 项目约定：日语本写入后自动查重（重复则删本次写入项并报告，同形异性词不算重复）、英语本中英自动补全、中译英需用简单易懂的词与语法
- 补全 `word.md` 105 条缺失读音/释义，删除 5 条重复占位行（提交 `f6667b4`）

### 每日额度按单词本分离

> **更正（v2.3）**：下面这句"学习进度全部独立，互不干扰"当时不成立。额度键和当日
> 计数确实分开了，但 `word_progress` 的主键仍是 `jp` 单列 —— 只加了 `book` 列却没改
> 主键，`save_progress` 的 upsert 仍按 `jp` 冲突合并，同一条文本在两本之间会互相覆盖。
> 真正做到进度独立是在 v2.3 把主键改成 `(jp, book)` 之后。

日语本与英语本的每日新词/复习额度、当日计数、学习进度全部独立，互不干扰。

- **`app.py`** —— 新增 `resolve_book()` 与 `get_limit(key, book)`；`get_progress` / `get_today_new_count` / `get_today_review_count` / `save_progress` 全部按 `book` 过滤或写入。五个路由（`/api/settings`、`/api/next`、`/api/review`、`/api/mastery`、`/api/review_today`）均从 query 或 body 取 `book` 并透传
- **额度键名** —— 由全局的 `new_words_limit` 改为分书的 `new_words_limit_{book}`。日语本在分书键缺失时回落到旧全局键，保证老用户原有设置不丢
- **`schema.sql`** —— 新增 v2.2 迁移段：`word_progress` 增 `book TEXT NOT NULL DEFAULT 'jp'`（存量行自动归日语本）、`(book, last_reviewed)` 复合索引、四个分书额度键（`_jp` 的值从旧全局键复制）。迁移幂等，可重复执行
- **`index.html`** —— 六处请求携带 `currentBook`；设置弹窗标题区分「每日设置 · 日语 / · English」，保存后自动刷新统计行

### 验证
- `app.py` 语法检查通过；grep 确认无遗漏的无参调用
- 模板渲染通过，六处 `book` 传递点逐一核对
- 迁移已在 Supabase SQL Editor 执行：`SELECT book, count(*) FROM word_progress GROUP BY book` 仅返回 `jp` 一行，确认存量进度全部正确归属日语本，无数据丢失

> **更正（v2.3）**：上面这条"验证"是无效的。`ADD COLUMN book TEXT NOT NULL DEFAULT 'jp'`
> 保证了迁移后每一行的 `book` 都是 `'jp'`，所以 `GROUP BY book` **必然**只返回一行 —— 这条
> 查询恒真，不具备证伪能力，把它的结果当成"归属正确"是循环论证。真正要查的是有没有
> 英语本的行被 `DEFAULT 'jp'` 误标（`c4a3d51` → `7c9c805` 之间约 19 小时里，英语本已能出题
> 但后端还不认 `book`，那期间写入的行没有 `book` 列）。v2.3 用 `strpos(jp, '\n') > 0` 和
> `length(jp) > 20` 两种判据实查过，库里没有这种脏数据，结论侥幸成立，但当时的验证方法不成立。
>
> 教训：验证语句必须能在"有问题"时给出不同的结果，否则它验证的是自己而不是数据。

### 清理
- 删除 v1.0 遗留的孤儿键 `daily_limit`（v2.0 拆分新词/复习额度后即无代码读取；同名字符串仅作为 `/api/next` 的响应原因，与该表行无关）
- 无后缀的 `new_words_limit` / `review_words_limit` 暂时保留作回滚备份，待新版本线上稳定后再删

## v2.1 — 2026-07-19

### 修复
- 修复新词分类轮询失效的问题：`/api/next` 之前每次请求都重新计算轮询列表、只取第一个，导致轮询永远从文档里排最前的分类（如"名词"）出词，实际上要把该分类学完才会轮到下一个。现在改用当天已学新词数 `today_new_count` 对当前非空分类数取模，作为跨请求的轮询游标，确保各分类真正轮流出现

### 验证
- 用离线测试脚本（内存假 Supabase 客户端 + 虚构测试词，不连接真实数据库）跑通了斩词功能全流程：打分升级 → 满级弹出确认 → 调用斩词接口 → 数据库标记 mastered → 之后无论是否到期都不再出现；并做了反向验证确认测试本身有效

## v2.0 — 2026-07-09

### 已完成
- 新词/复习词每日额度分开设置，复习优先于新词
- 每个词记录累计学习次数（`review_count`），用于区分"今天是新学还是复习"
- 斩词：熟悉度满格时可选择永久退场，不再出现
- 继续学习：今日额度用完后可取消限制，继续学完当天已到期的词 ⚠️ 见下方 v2.3 更正
- 自由复习：完成后可回顾今天学过的词，不影响 SRS 进度 ⚠️ 见下方 v2.3 更正

> **更正（v2.3）**：上面最后两条实际上**交付起就没有工作过**，直到 v2.3 才真正可用。
> 两个按钮都只能从"额度已用完 / 复习完了"画面进入，而那张画面用
> `card.innerHTML = ...` 覆盖了整张卡片、把 `#answer` 等元素从 DOM 删掉；
> 点按钮后走到 `loadNext()` / `showFreeReviewCard()` 里访问 `#answer` 就抛异常中断。
> 已在 `330e7e4` 逐行核实。
>
> 「继续学习」看起来"有时能用"，是因为点击已经把标记写进 localStorage 了，
> 只是画面没刷新；用户手动刷新页面后它就生效，于是缺陷被掩盖了三个月。

---

## v1.0 — 2026-06-29

### 已完成
- 基于 SRS 算法的翻牌式复习（不会 / 模糊 / 记住了，间隔 1→3→7→14→30 天）
- 每日复习量上限，UI 内可直接设置
- 进度云端持久化（Supabase PostgreSQL）
- 自动部署（GitHub → Render）
- 自定义词源（Markdown 文件）
- 熟悉度可视化（橙点进度条）

---

## 未来功能

### 多词库切换（v2.3 已完成）
- ~~UI 添加词库选择器~~ 已实现为「日语 / English」tab
- ~~支持多个 Markdown 数据源文件~~ 已实现 `word.md` / `word_en.md`
- ~~`word_progress` 表新增区分字段（实现时定名为 `book`），各词库进度与额度独立记录~~
  v2.2 加了 `book` 列和分本额度键，但主键仍是 `jp` 单列、进度仍会互相覆盖；
  v2.3 把主键改成 `(jp, book)` 后才真正独立
- 后续可继续扩展的场景：数学公式/定理、法律条文、游戏台词、任何需要记忆的内容

### 统计面板
- 今日/本周/本月复习量图表
- 各熟悉度等级分布
- 连续打卡天数

### 词条管理
- 直接在 UI 里增删改词条，不需要手动编辑 Markdown 再 push
- 支持给词条添加备注/例句

### 导入功能
- 从 CSV / Anki 导出文件导入词条

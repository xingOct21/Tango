# 项目约定

本文件由 Claude Code 每次会话自动读取。以下规则**无需用户重复提示，自动执行**。

## 两个单词本

| 文件 | 用途 | 列结构 |
|------|------|--------|
| `word.md` | 日语单词本 | 日语 / 假名 / 中文 |
| `word_en.md` | 毕业答辩问答本 | 问题 / 答案 / 中文 |

段落换行统一用字面量 `\n`（前端 `renderText()` 会转成 `<br>`）。

---

## 规则一：日语单词本写入后自动查重

每次向 `word.md` 写入词条后，**必须自动执行**下列检查，不等用户提出：

1. 以第一列（日语原词）为键，扫描整个文件找重复项。
2. 若本次写入的词条与文件中已有词条重复，**删除本次写入的那条**，保留原有条目。
3. 向用户报告：删掉了哪些词、与之重复的原条目在第几行。

**例外 —— 下列情况不算重复，两条都保留：**

- 同形词但词性不同、释义不同，且分处不同章节。
  例：`間抜け` 在「名词」章节是"傻瓜，呆子"，在「形容词」章节是"愚蠢的（な形）"。

---

## 规则二：英语答辩本中英对照

`word_en.md` 的**问题和答案都必须中英文对照**。用户只写了其中一种语言时，**自动补齐另一种**，不等用户提出。

三列的编码方式：

| 列 | 内容 |
|----|------|
| 问题 | 中文问题 `\n` 英文问题 |
| 答案 | 英文答案 |
| 中文 | 中文答案 |

### 中译英要点

把中文翻成英文时（用户没给英文、或只给了中文版），**优先选用简单易懂的单词、措辞和语法**：

- 用常见词，不用生僻词和花哨的学术词汇
- 句子短，一句话讲一件事，避免从句套从句
- 语法结构直白，主谓宾清楚
- 目标：答辩现场能顺畅说出口，评委一听就懂
